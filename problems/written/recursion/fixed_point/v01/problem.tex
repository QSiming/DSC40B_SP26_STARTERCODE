\begin{prob}
   Given a sorted array of distinct integers A[1 . . . n], give an algorithm to find out whether there is an index i for which A[i] = i. Analyze the time complexity of the algorithm. 
   \\
   (Hint : There is a solution that runs in better than linear $\Theta(n)$ time!)
    \begin{soln}
        \inputminted{python}{\thisdir/include/soln.py}
        The time complexity of the algorithm is $\theta(log n)$.
    \end{soln}  
\end{prob}
