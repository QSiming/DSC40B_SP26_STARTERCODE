\begin{prob}
    We're given two lists, \python{A} and \python{B} and a target \python{t}, and our goal is to find an element $a$ of \python{A} and an element $b$ of \python{B} such that $a + b = t$.
    
    \begin{soln}
        \inputminted{python}{\thisdir/include-solution/target_sum.py}

    \end{soln}
\end{prob}
