%% tags: [graph theory, shortest path]
%% source: 2023-fa-midterm_02
\begin{prob}

    Suppose an undirected, connected graph has $n$ nodes, half of which are
    colored red and the other half are colored blue. Each red node is neighbors
    with $n/4$ blue nodes, and each blue node is neighbors with $n/4$ red
    nodes. There are no edges between nodes of the same color.

    What is the time complexity of computing a shortest path from a given red
    node to all other red nodes using BFS?

    \begin{choices}
        \choice $\Theta(1)$
        \choice $\Theta(n)$
        \choice $\Theta(n \log n)$
        \correctchoice $\Theta(n^2)$
    \end{choices}

    \begin{soln}
        $\Theta(n^2)$
    \end{soln}


\end{prob}
