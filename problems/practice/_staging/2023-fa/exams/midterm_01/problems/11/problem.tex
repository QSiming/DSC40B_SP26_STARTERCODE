\begin{prob}
    Suppose $f_1(n)$ is $O(n^3)$ and $\Omega(n)$. Also suppose that $f_2(n) = O(n^2)$ and $\Omega(\sqrt n)$.

    Consider the function $f(n) = f_1(n) + f_2(n)$. True or false: it must be 
    the case that $f(n) = \Omega(n)$.

    \Tf{}
\end{prob}
