\begin{prob}
    Consider again the \python{mediansort} function from above. Suppose that \python{find_median}
    takes $\Theta(n)$ time. What is the time complexity of \python{mediansort}?

    \inlineresponsebox{$\Theta(n \log n)$}
\end{prob}
