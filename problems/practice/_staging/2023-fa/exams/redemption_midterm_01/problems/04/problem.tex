\Needspace{2in}
\begin{prob}

    Suppose \texttt{numbers} is a list of integers of length $n$. What is the time complexity of the following function in terms of $n$?
    State your answer using asymptotic notation (e.g., $\Theta(n)$) in the simplest
    terms possible.

    \inputminted{python}{\thisdir/code.py}

    \inlineresponsebox{$\Theta(n^2)$}
\end{prob}
