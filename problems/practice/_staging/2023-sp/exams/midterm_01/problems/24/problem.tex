%% source: 2023-sp-midterm_01
\begin{prob}
    Consider the modification of \python{mergesort} shown below, where one of the
    recursive calls has been replaced by an in-place version of
    \python{selection_sort}. Recall that \python{selection_sort} takes $\Theta(n^2)$
    time.

    \inputminted{python}{\thisdir/code.py}

    What is the time complexity of \python{kinda_mergesort}?

    \inlineresponsebox{$\Theta(n^2)$}
\end{prob}
