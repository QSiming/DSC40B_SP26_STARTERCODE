%% source: 2023-sp-midterm_01
\begin{prob}
    Consider \python{iterative_binary_search} from above and note the \python{print}
    statement in the \python{while}-loop. Suppose \python{iterative_binary_search}
    is run on the array:
    \[
        \python{[-202, -201, -200, -50, -20, -10, -4, -3, 0, 1, 3, 5, 6, 7, 9, 10, 12, 15, 22]}
    \]
    with target \python{11}.

    What will be the last value of \python{arr[start]} printed?

    \inlineresponsebox[.5in]{10}
\end{prob}
