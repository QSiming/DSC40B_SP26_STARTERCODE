%% source: 2023-sp-redemption_midterm_01
\begin{prob}
    State (but do not solve) the recurrence describing the run time of the following code,
    assuming that the input, \python{arr}, is a list of size $n$.

    \inputminted[lastline=10]{python}{\thisdir/code.py}

    $T(n) =$ \inlineresponsebox[3in]{$T(n-1) + \Theta(1)$}

\end{prob}
