%% source: 2023-sp-midterm_02
\begin{prob}

    Suppose an undirected graph $G = (V, E)$ is a tree with 33 edges. Suppose a node $s$ is used as the source
    of a depth-first search.

    Including the root call of \python{dfs} on node $s$, how many calls to \python{dfs} will be made?

    \inlineresponsebox[.75in]{34}

\end{prob}
