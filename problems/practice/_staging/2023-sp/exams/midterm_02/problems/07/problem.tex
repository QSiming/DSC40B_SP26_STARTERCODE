%% source: 2023-sp-midterm_02
\begin{prob}
    Let $v = \{a, b, c, d, e, f, g\}$ and $E = \{(a,g), (b,d), (d, e), (f, d)\}$.
    How many connected components does the undirected graph $G =(V, E)$ have?

    \inlineresponsebox[.75in]{3}

\end{prob}
