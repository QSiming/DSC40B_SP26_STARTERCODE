%% source: 2023-sp-midterm_02
\begin{prob}
    Consider calling \python{full_bfs} on a \emph{directed} graph, and recall
    that \python{full_bfs} will make calls to \python{bfs} until all nodes have been marked as visited.

    True or False: the number of calls made to \python{bfs} depends on the order in which \python{graph.nodes}
    produces the graph's nodes. That is, if nodes are produced in a different order, the number of calls to \python{bfs}
    may be different.

    \Tf{}

\end{prob}
