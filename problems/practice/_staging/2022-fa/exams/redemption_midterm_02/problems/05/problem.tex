\begin{prob}
    Let $G = (V, E)$ be an undirected graph with $V = \{a, b, c, d, e, f, g, h\}$
    and $E = \{(a, b), (d, c), (f, e)\}$. How many connected components does $G$ have?

    \inlineresponsebox[1.5in]{5}

\end{prob}
