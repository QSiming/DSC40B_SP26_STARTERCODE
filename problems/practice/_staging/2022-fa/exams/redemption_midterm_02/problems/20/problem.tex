\begin{prob}
    Suppose that an $n \times n$ adjacency matrix $A$ representing an undirected
    graph $G$ has a row with $n - 1$ ones (the diagonal of $A$ contains all zeros, since
    the graph is undirected and thus has no self-loops).

    True or False: $G$ is connected.

    \Tf{}

\end{prob}
