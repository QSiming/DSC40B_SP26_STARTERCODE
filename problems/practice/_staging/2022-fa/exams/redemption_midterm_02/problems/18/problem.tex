\begin{prob}
    Suppose a DFS is run on a \textbf{directed} graph. True or False: it is possible that,
    at some moment in time, there is a node marked as ``undiscovered'' which has a neighbor
    node that is marked as visited.

    \Tf{}

\end{prob}
